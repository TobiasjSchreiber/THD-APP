1. Modify `animations.js` to calculate distance to the top edge of `widget-weather`.
2. Add `--fall-distance` to `.rain-drop` and use it in `@keyframes`.
3. Add `@keyframes rain-bounce` to `.rain-drop` if it intersects the widget horizontally.
4. Add animations to `.lens-flare-circle` elements to make them move around.
